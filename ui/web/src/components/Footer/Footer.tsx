import { Box } from "@mui/material";

export default function Footer() {
    return (
        <Box component="footer" sx={{ p: 4, mt: 'auto', display: 'flex', justifyContent: 'center' }}>
            <h6>temporary footer...</h6>
        </Box>
    )
}
